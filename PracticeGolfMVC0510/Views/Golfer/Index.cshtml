@model IEnumerable<PracticeGolfMVC0510.Models.Golfer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.strFirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.strLastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.strNickname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.strFirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.strLastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.strNickname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.intPersonID }) |
            @Html.ActionLink("Details", "Details", new { id=item.intPersonID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.intPersonID })
        </td>
    </tr>
}

</table>
